Before run the program, please rename the input data as "dataset" ,
and run the command " sh split.sh", it will split the dataset and create the input file.
then you can run the swift program by typing "swift wordCount.swift"   